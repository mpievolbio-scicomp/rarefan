image: mpievolbioscicomp/repinpop_base

# Disable the Gradle daemon for Continuous Integration servers as correctness
# is usually a priority over speed in CI environments. Using a fresh
# runtime for each build is more reliable since the runtime is completely
# isolated from any previous builds.
variables:
  GRADLE_OPTS: "-Dorg.gradle.daemon=false"

stages:
  - build
  - test

build:
  stage: build
  script:
    - conda init bash
    - source ~/.bashrc
    - conda activate repinpop
    - export INSTALL_PREFIX=$(echo $CONDA_PREFIX)
    - mkdir build
    - cd build
    - CC=/usr/bin/gcc cmake -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX ..
    - make
    - cd ..
    - cd REPIN_ecology/REPIN_ecology 
    - gradle build
    - cd ../..
  artifacts:
    paths:
      - ~/.bashrc
      - REPIN_ecology/REPIN_ecology/build/REPIN_ecology.jar

test:
  stage: test
  script: 
    - source ~/.bashrc
    - conda activate repinpop
    - source setenv.sh
    - ./run_test.sh -d neisseria_small
